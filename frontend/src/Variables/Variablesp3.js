import React from 'react';
import { Link } from "react-router-dom";

function Variablesp3(props) {
    // const routeChange = () => {
    //     let path = `/variablesp4`;

    return (
        <div>

        <p>The new version of JavaScript allows the use of the const keyword to define a variable that cannot be reassigned, and the let keyword to define a variable with restricted scope.</p>


            {/* <div>
        <button
          style={{ display: "flex", alignItems: "flex-end" }}
          onClick={routeChange}>{" "}
          <Link to="/variablesp4">Next Slide</Link>{" "}
        </button>
      </div> */}
        </div>
        
    );
}

export default Variablesp3;